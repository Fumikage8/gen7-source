テクスチャが貼られていないところの処理をどうするか
（WRAP, MIRROR, CLAMP, BORDER のどれを指定すればよいか）

    ・高輝度を抽出される画像（フレームバッファ）
        BORDER(別途指定している色(黒))

    ・抽出した高輝度に掛けるアルファマスク画像
        CLAMP(テクスチャの端の色を使う)

    ・ブラーを加算合成する板ポリに貼る画像（高輝度をぼかした画像）
        BORDER(別途指定している色(黒))

以上。